name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:

  static-analysis:
    uses: ./.github/workflows/reusable-static-analysis.yml

  tests:
    uses: ./.github/workflows/reusable-unit-tests.yml
